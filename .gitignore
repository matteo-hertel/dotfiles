bundle
.netrwhist
.vim/view

# Per-machine env (API keys, workspace aliases)
.env/env.custom.sh

# Colorsync runtime (generated files, backups)
.config/colorsync/output/
.config/colorsync/backup/

# Build artifacts
tools/colorsync/colorsync

# Per-machine Claude config (each machine has its own imports)
.claude/CLAUDE.md
